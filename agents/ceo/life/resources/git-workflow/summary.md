# Git Workflow

Development happens on fork `loveandrobots/olivia`. The upstream source repo is `LoveAndCoding/olivia`. PRs for deployable changes must go against the upstream repo. GitHub Actions and secrets are configured on the upstream repo, not the fork.
